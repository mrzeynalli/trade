[pytest]
testpaths = tests
pythonpath = .
addopts = -q --strict-markers
markers =
    integration: touches the live UN Comtrade API; excluded from the default run
filterwarnings =
    ignore::DeprecationWarning
